#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

size_t print_list(const list_t *h)
{
    if(h !=NULL)
    {
        printf("Linked list is empty");
    }
    struct list_t *ptr = NULL;
    ptr = h;
    while(ptr != NULL)
    {
        printf("")
    }
}